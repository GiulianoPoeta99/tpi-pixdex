import { RegisterScreen } from '@/src/auth/screens/RegisterScreen';

/**
 * Pantalla de registro de usuarios.
 * Renderiza el componente RegisterScreen que permite a los usuarios
 * crear una nueva cuenta en la aplicación.
 *
 * @component
 * @returns {JSX.Element} Pantalla de registro.
 *
 * @example
 * <RegisterScreen />
 */
export default RegisterScreen;
